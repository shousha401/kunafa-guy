# Plan — The Kunafah Guy Build Spec

Single-deliverable spec document (no codebase, per request).

- Stage 1: Write full build spec → `/mnt/agents/output/kunafah-guy-build-spec.md`
  - File tree
  - site.config.ts schema (known data filled, UNCONFIRMED flags)
  - Tailwind tokens (food-derived palette, type stack, spacing/shadow)
  - Component contracts (prop interfaces)
  - Per-section build cards w/ acceptance criteria
  - Asset manifest (slots, ratios, filenames)
  - OPEN QUESTIONS list
- Stage 2: Deliver file + brief summary. No subagents needed — one cohesive design doc.
